{% extends 'base.html.twig' %}

{% block title %}Modifier !{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-2">Modifier !</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-warning">Modifier !</button>
            <a href="{{ path('home') }}" class="btn btn-warning">Retourner</a>
        {{ form_end(form) }}
    </div>
{% endblock %}