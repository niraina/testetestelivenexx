{% extends 'base.html.twig' %}

{% block title %}Nouveau !{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center my-2">Nouveau !</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">Enregistrer</button>
            <a href="{{ path('home') }}" class="btn btn-warning">Retourner</a>
        {{ form_end(form) }}
    </div>
{% endblock %}